{block content}

{control breadCrumb}

<section class="content">
    <h1 n:block="title">Kolá</h1>
</section>

<div class="table-responsive" n:if="$rounds->count()">
    <table class="table">
        <tbody>
            <tr n:foreach="$rounds as $round">
                <td>
                    <a n:href="archView, $round->archive_id, $round">{$round->name}</a>
                </td>
            </tr>
        </tbody>
    </table>
</div>

{/block}